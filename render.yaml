services:
  - type: web
    name: aq50-autism-test
    runtime: static
    staticPublishPath: .
    buildCommand: echo "No build needed"
    envVars:
      - key: SKIP_INSTALL_DEPS
        value: "true"
